namespace NonsensicalKit.Simulation.WarehouseSimulation.Config
{
    /// <summary>货物流向：入库（外部 → 货架）或出库（货架 → 外部）。</summary>
    public enum SimFlowDirection
    {
        Inbound = 0,
        Outbound = 1,
    }
}
